// Landing page lives at src/app/page.tsx — delete this file.
// It is here only because the folder structure was pre-created.
// Keeping an empty default export avoids a missing-default-export error,
// but you MUST delete this file to prevent a Next.js duplicate-route error at build time.
export default function Placeholder() { return null; }
